steps:
# Build the alpine and debian versions.
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-f', 'docker/Dockerfile.trained', '--tag=gcr.io/$PROJECT_ID/trained', '.']
images: ['gcr.io/$PROJECT_ID/trained']